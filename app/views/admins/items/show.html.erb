<h2>商品詳細</h2>
<div class="image"><%= attachment_image_tag @admin, :image, :fill, 250, 200, format:'jpeg', fallback: "PAK57_ichigonocake_TP_V4 (1).jpg", size:'250x200' %></div>
<div class="form-group">
  <label>商品名</label>
  <div><%= @item.name %></div>
</div>
<div class="form-group">
  <label>商品説明</label>
  <div><%= @item.introduction %></div>
</div>
<div class="form-group">
  <label>ジャンル</label>
  <div><%= @item.genre.name %></div>
</div>
<div class="form-group">
  <label>税込価格(税抜価格)</label>
  <div><%= @item.taxed_price.floor %>(<%= @item.price %>)</div>
</div>
<div class="form-group">
  <label>販売ステータス</label>
  <div><%= @item.is_active ? '販売中' : '売り切れ' %></div>
</div>
<div>
    <%= link_to "編集する", edit_admins_item_path(@item) %>
</div>
