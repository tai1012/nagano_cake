<h2>注文情報確認</h2>
<table>
  <thead>
    <tr>
      <td> </td>
      <td>商品名</td>
      <td>単価(税込)</td>
      <td>数量</td>
      <td>小計</td>
    </tr>
  </thead>
  <tbody>
    <% @cart_items.each do |cart_item| %>
    <tr>
      <td>
        <%= attachment_image_tag(cart_item.item, :image, :fill, 60, 40, size: '60x40') %>
        <%= cart_item.item.name %>
      </td>
      <td>
        <%= cart_item.item.taxed_price.to_s(:delimited)%>
      </td>
      <td>
        <%= cart_item.item.amount %>
      </td>
    </tr>
  </tbody>    
      
      
      <td>
        <%= (cart_item.item.taxed_price * cart_item.amount).to_s(:delimited) %>
      </td>