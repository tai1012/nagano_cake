<h2>配送先編集</h2>
<%= form_with model: @address, local:true do |f| %>
<div>
  <%= f.label "郵便番号(ハイフンなし)" %>
  <%= f.text_field :postal_code, value: @address.postal_code %>
</div>
<div>
  <%= f.label "住所" %>
  <%= f.text_field :address, size:50, value: @address.address %>
</div>
<div>
  <%= f.label "宛名" %>
  <%= f.text_field :name, value: @address.name %>
</div>
  <%= f.submit "変更内容を保存" %>

<% end %>