<h2>マイページ</h2>
<div>登録情報</div>
<%= link_to "編集する", edit_customers_path(current_customer.id) %>
<div>
  <table>
    <tbody>
      <tr>
        <th>氏名</th>
        <td><%= current_customer.first_name %> <%= current_customer.last_name %></td>
        <th>カナ</th>
        <td><%= current_customer.first_name_kana %> <%= current_customer.last_name_kana %></td>
        <th>郵便番号</th>
        <td><%= current_customer.postal_code %></td>
        <th>住所</th>
        <td><%= current_customer.address %></td>
        <th>電話番号</th>
        <td><%= current_customer.telephone_number %></td>
        <th>メールアドレス</th>
        <td><%= current_customer.email %></td>
      </tr>
    </tbody>
  </table>
</div>
<div>配送先</div>
<%= link_to "一覧を見る", addresses_path %>
<div>注文一覧</div>
<%= link_to "一覧を見る" %>